--------------------------------------------------------------------------------
                                      Fix                                       
--------------------------------------------------------------------------------

* connection parse_spawn_command
    * Fixed Although `login_creds='local'` is specified, `default` is selected


